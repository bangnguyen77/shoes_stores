<h1><%= @store.name() %></h1>

<% if @store.brands.any? %>
  <h3>Brands:</h3>
  <ul>
    <% @store.brands.each() do |store_brand| %>
      <li><%= store_brand.name() %></li>
    <% end %>
  </ul>
<% else %>
  <p>
    There are currently no brands in this store.
  </p>
<% end %>
